Applications of linked list:
1. Implements queues and stacks
2. In web-browsers, where it creates a linked list of web pages visited, so that when you check history(traversal of a list) or press back button, the previous node's data is fetched.